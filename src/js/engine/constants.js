const fontWithSize = (size) => `${size ?? 12}px 'Press Start 2P'`;

export const constants = {
  primaryColor: '#12ce5d',
  backgroundColor: '#000000',
  windowHeight: 320,
  windowWidth: 480,
  font: fontWithSize(12),
  fontWithSize,
  textAlign: 'center',
};
